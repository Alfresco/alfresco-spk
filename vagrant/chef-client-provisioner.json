{
  "type": "chef-solo",
  "execute_command" : "cd /etc/chef && {{if .Sudo}}sudo {{end}}chef-client --no-color -z -j {{.JsonPath}}",
  "install_command": "sudo bash -c 'curl -L https://www.opscode.com/chef/install.sh| bash -s -- -v 12.5.1'",
  "prevent_sudo": false,
  "skip_install": false,
  "cookbook_paths": ["../cookbooks"]
}
